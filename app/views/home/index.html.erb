<h1>Home</h1>
<div class="left-side">
  <%= render '/home/form' %>
</div>
<div class="right-side">
  <h5 id="counter"><%= @counter %></h5>
  <div id="informer">
    <% @lines.reverse.each do |l| %>
    <div class="entry"><%= l.created_at %></div>
    <% end %>
  </div>
</div>
